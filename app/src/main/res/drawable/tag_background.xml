<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 배경 색상 설정 -->
    <solid android:color="@color/Light_Gray" /> <!-- 배경 투명 -->
    <!-- 가장자리 테두리 설정 -->
    <stroke
        android:width="1dp"
        android:color="@android:color/white" />
    <!-- 모서리 둥글게 설정 -->
    <corners android:radius="10dp" />
</shape>