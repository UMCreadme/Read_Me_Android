<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 버튼이 눌렸을 때 (팔로잉 상태) -->
    <item android:state_selected="true" android:drawable="@drawable/btn_pf_following" />
    <!-- 기본 상태 (팔로우 상태) -->
    <item android:drawable="@drawable/btn_pf_follow" />
</selector>
